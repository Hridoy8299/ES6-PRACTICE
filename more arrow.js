const add = (first, second)=> first +second;
const getFullNanme = (first, second)=> first + second;
const multiply =(a, b)=> a*b ;

// No parameter
const Getpie = ()=>3.1416;

const getPie=(one)=> one*2;

// ome parameter simple version

const fiveTimes = num => num*5;

// multi line arrow function
// multiline arrow function jonno { } use korte hobe and return lekhte hobe 
 const doMath = (x,y,z)=> {
    const firstSum = x+y;
    const secondSum = y+z;
    const multiplr = firstSum * secondSum;
    const result = multiline/2;
    return result;
}

